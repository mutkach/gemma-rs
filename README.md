# gemma.rs

### A Rust + Metal implementation of Llama/Gemma/Qwen-type models

1. Use `safetensors` and `mmap` for quick loading
2. Use `metal-rs` for interacting with Metal pipelines
3. ???

deps:
```
bytemuck = "1.25.2"
memmap2 = "0.9.11"
metal-rs = "0.10.6"
safetensors = "0.8.0"
```
